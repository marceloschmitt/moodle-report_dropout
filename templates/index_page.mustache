<div class="hero-unit">
<div class="badge badge-info">Estes dados são simulados apenas para teste da interface!</info>
    <table class="table table-striped">
        <tbody>
        {{#students}}
            <tr>
                <td scope="row"><span class="badge badge-{{riskclass}}">{{riskprint}}</span></td>
                <td><a href="report.php?id={{courseid}}&userid={{id}}&risk={{risk}}"> {{firstname}} {{lastname}} ({{username}}) - {{email}}</a></td>
            </tr>
        {{/students}}
        </tbody>
    </table>
</div>
