<div class="hero-unit">
    <table class="table table-striped">
        <tbody>
        {{#students}}
            <tr>
                <td scope="row"><span class="badge badge-warning">{{risk}}</span></td>
                <td><a href="report.php?id={{courseid}}&userid={{id}}"> {{firstname}} {{lastname}} ({{username}}) - {{email}}</a></td>
            </tr>
        {{/students}}
        </tbody>
    </table>
</div>
